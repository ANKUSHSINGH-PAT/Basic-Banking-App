<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.basic_banking_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Basic_Banking_APP">
        <activity android:name=".UI.welcome_screen"></activity>
        <activity android:name=".UI.SendToUser" />
        <activity android:name=".UI.UserData" />
        <activity android:name=".UI.UsersList" />
        <activity android:name=".UI.Transaction_history" />
        <activity android:name=".UI.HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>